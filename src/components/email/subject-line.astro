---
const { subject, from } = Astro.props;
---

<div class="px-4 mb-8 max-w-md w-full">
  <div class="py-6">
    <p class="text-sm font-medium text-gray-900">From: {from}</p>
    <p class="mt-1 truncate text-sm text-gray-500">Subject: {subject}</p>
  </div>

  <div class="relative">
    <div class="absolute inset-0 flex items-center" aria-hidden="true">
      <div class="w-full border-t border-gray-300"></div>
    </div>
  </div>
</div>
