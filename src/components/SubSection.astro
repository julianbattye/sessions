---
const { title, description } = Astro.props;
---

<div class="flex flex-col sm:flex-row gap-6 pt-8 mt-8 first-of-type:mt-0">
  <div class="sm:w-1/3">
    {title && <h3 class=" text-base font-medium text-gray-700">{title}</h3>}
  </div>
  <div class="sm:w-2/3 text-gray-700 text-sm"><slot /></div>
</div>
